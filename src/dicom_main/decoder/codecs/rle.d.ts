/* eslint-disable no-bitwise */
/* eslint-disable no-plusplus */

import { IDisplayInfo } from "@/dicom_main/image/DisplayInfo";

function decode(frameInfo:IDisplayInfo, pixelDataView:DataView):DataView

export default decode;
